/* pages/wallet/index.wxss */
.container {
  display: flex;
  flex-direction: column;
  min-height: 100vh;
  background: linear-gradient(180deg, #0f0f23 0%, #1a1a2e 30%, #16213e 100%);
  position: relative;
}

.main-scroll { flex: 1; }

/* ── 余额卡 ── */
.balance-card { padding: 24rpx 24rpx 0; }

.balance-card-inner {
  background: linear-gradient(145deg, #1a1a2e 0%, #16213e 100%);
  border-radius: 28rpx;
  padding: 40rpx 36rpx 36rpx;
  box-shadow: 0 16rpx 40rpx rgba(0, 0, 0, 0.3);
  border: 1rpx solid rgba(0, 210, 255, 0.15);
}

.balance-label {
  display: block;
  font-size: 22rpx;
  color: rgba(255, 240, 220, 0.55);
  margin-bottom: 16rpx;
}

.balance-amount-row {
  display: flex;
  align-items: baseline;
  gap: 8rpx;
  margin-bottom: 12rpx;
}

.balance-symbol { font-size: 36rpx; color: rgba(0, 210, 255, 0.6); font-weight: 600; }

.balance-amount {
  font-size: 80rpx;
  font-weight: 700;
  color: #00d2ff;
  line-height: 1;
  text-shadow: 0 0 20rpx rgba(0, 210, 255, 0.4), 0 0 40rpx rgba(0, 210, 255, 0.2);
}

.balance-meta-row { margin-bottom: 28rpx; }

.balance-meta-text { font-size: 22rpx; color: rgba(212, 184, 150, 0.70); }

.balance-action-row { display: flex; gap: 16rpx; }

.withdraw-btn {
  min-width: 200rpx;
  height: 80rpx;
  border-radius: 999rpx;
  background: rgba(0, 210, 255, 0.15);
  border: 1rpx solid rgba(0, 210, 255, 0.4);
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 0 40rpx;
}

.withdraw-btn-text { font-size: 28rpx; font-weight: 600; color: #00d2ff; }

.withdraw-history-btn {
  min-width: 168rpx;
  height: 80rpx;
  border-radius: 999rpx;
  background: rgba(123, 47, 247, 0.1);
  border: 1rpx solid rgba(123, 47, 247, 0.3);
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 0 32rpx;
}

.withdraw-history-btn-text {
  font-size: 26rpx;
  font-weight: 600;
  color: rgba(167, 139, 250, 0.82);
}

/* ── 通用卡片 ── */
.section-card {
  margin: 16rpx 24rpx 0;
  background: rgba(22, 33, 62, 0.6);
  backdrop-filter: blur(16px);
  -webkit-backdrop-filter: blur(16px);
  border-radius: 24rpx;
  padding: 28rpx;
  box-shadow: 0 8rpx 24rpx rgba(0, 0, 0, 0.2);
  border: 1rpx solid rgba(0, 210, 255, 0.08);
}

.section-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 20rpx;
}

.section-title {
  display: block;
  font-size: 28rpx;
  font-weight: 700;
  color: rgba(255, 255, 255, 0.9);
  margin-bottom: 20rpx;
}

.section-title-inline { margin-bottom: 0; }

.section-more {
  font-size: 24rpx;
  color: #A06C46;
}

/* ── 佣金概览四格 ── */
.overview-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 16rpx;
}

.overview-item {
  background: rgba(0, 210, 255, 0.06);
  border: 1rpx solid rgba(0, 210, 255, 0.1);
  border-radius: 16rpx;
  padding: 20rpx;
  display: flex;
  flex-direction: column;
  gap: 8rpx;
}

.overview-val { font-size: 34rpx; font-weight: 700; color: rgba(255, 255, 255, 0.9); }
.overview-frozen { color: #f59e0b; }
.overview-estimated { color: #a78bfa; }
.overview-pending { color: #f97316; }
.overview-settled { color: #34d399; }
.overview-key { font-size: 22rpx; color: rgba(255, 255, 255, 0.5); }
.overview-tip { font-size: 18rpx; color: rgba(255, 255, 255, 0.35); line-height: 1.5; }

/* ── 待入账展开卡 ── */
.pending-card {
  margin: 16rpx 24rpx 0;
  background: rgba(22, 33, 62, 0.6);
  backdrop-filter: blur(16px);
  -webkit-backdrop-filter: blur(16px);
  border-radius: 24rpx;
  padding: 24rpx 28rpx;
  box-shadow: 0 8rpx 24rpx rgba(0, 0, 0, 0.2);
  border: 1rpx solid rgba(0, 210, 255, 0.08);
  overflow: hidden;
}

.pc-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.pc-header-left { flex: 1; }

.pc-title { display: block; font-size: 28rpx; font-weight: 700; color: rgba(255, 255, 255, 0.9); }

.pc-subtitle { display: block; font-size: 22rpx; color: rgba(255, 255, 255, 0.4); margin-top: 4rpx; }

.pc-header-right {
  display: flex;
  align-items: center;
  gap: 10rpx;
  flex-shrink: 0;
}

.pc-total { font-size: 36rpx; font-weight: 700; color: #f97316; }

.pc-arrow {
  font-size: 36rpx;
  color: rgba(255, 255, 255, 0.4);
  display: inline-block;
  transition: transform 0.25s;
  transform: rotate(0deg);
}

.pc-arrow-up { transform: rotate(90deg); }

.pc-detail {
  max-height: 0;
  overflow: hidden;
  transition: max-height 0.3s ease;
}

.pc-detail-open { max-height: 520rpx; }

.pc-divider {
  height: 1rpx;
  background: rgba(122, 108, 94, 0.10);
  margin: 20rpx 0 16rpx;
}

.pc-row {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  padding: 14rpx 0;
}

.pc-row-left {
  display: flex;
  align-items: flex-start;
  gap: 14rpx;
  flex: 1;
}

.pc-dot {
  width: 16rpx;
  height: 16rpx;
  border-radius: 50%;
  flex-shrink: 0;
  margin-top: 8rpx;
}

.pc-dot-estimated { background: #8E6AC8; }
.pc-dot-frozen    { background: #B76447; }
.pc-dot-pending   { background: #D4965A; }
.pc-dot-settling  { background: #5A8060; }

.pc-row-label { display: block; font-size: 26rpx; font-weight: 600; color: #1C1917; }

.pc-row-hint { display: block; font-size: 20rpx; color: #B0A396; margin-top: 4rpx; }

.pc-row-amount { font-size: 28rpx; font-weight: 700; flex-shrink: 0; padding-top: 2rpx; }
.pc-amount-estimated { color: #8E6AC8; }
.pc-amount-frozen   { color: #B76447; }
.pc-amount-pending  { color: #D4965A; }
.pc-amount-settling { color: #5A8060; }

/* ── 代理商货款入口 ── */
.goods-fund-entry {
  margin: 16rpx 24rpx 0;
  background: rgba(22, 33, 62, 0.6);
  backdrop-filter: blur(16px);
  -webkit-backdrop-filter: blur(16px);
  border-radius: 24rpx;
  padding: 24rpx 28rpx;
  box-shadow: 0 8rpx 24rpx rgba(0, 0, 0, 0.2);
  border: 1rpx solid rgba(0, 210, 255, 0.08);
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.gfe-left { display: flex; align-items: center; gap: 18rpx; flex: 1; }
.gfe-icon { font-size: 44rpx; }
.gfe-info { display: flex; flex-direction: column; }
.gfe-title { font-size: 28rpx; font-weight: 700; color: rgba(255, 255, 255, 0.9); }
.gfe-sub { font-size: 22rpx; color: rgba(255, 255, 255, 0.4); margin-top: 4rpx; }
.gfe-right { display: flex; align-items: center; gap: 8rpx; flex-shrink: 0; }
.gfe-balance { font-size: 30rpx; font-weight: 700; color: #f97316; }
.gfe-arrow { font-size: 32rpx; color: rgba(255, 255, 255, 0.4); }

/* ── 提现记录 ── */
.withdraw-skeleton { padding-top: 4rpx; }

.withdraw-record {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  padding: 22rpx 0;
  border-bottom: 1rpx solid rgba(122, 108, 94, 0.08);
}

.withdraw-record:last-child { border-bottom: none; }

.withdraw-record-main {
  flex: 1;
  min-width: 0;
  margin-right: 16rpx;
}

.withdraw-record-top {
  display: flex;
  align-items: center;
  gap: 10rpx;
  margin-bottom: 6rpx;
}

.withdraw-record-title {
  font-size: 28rpx;
  font-weight: 600;
  color: #1C1917;
}

.withdraw-record-status {
  font-size: 20rpx;
  padding: 4rpx 12rpx;
  border-radius: 999rpx;
}

.withdraw-status-pending { color: #A06C46; background: #F8EEE4; }
.withdraw-status-approved { color: #6A7E9A; background: #EEF4FB; }
.withdraw-status-processing { color: #8E6AC8; background: #F1ECFA; }
.withdraw-status-success { color: #53725B; background: #EDF6EF; }
.withdraw-status-fail { color: #B76447; background: #FAECE8; }
.withdraw-status-muted { color: #9A8D80; background: #F2EFEC; }

.withdraw-record-time {
  display: block;
  font-size: 22rpx;
  color: #B0A396;
  margin-bottom: 4rpx;
}

.withdraw-record-hint {
  display: block;
  font-size: 22rpx;
  color: #8B7355;
  line-height: 1.5;
}

.withdraw-record-reason {
  display: block;
  margin-top: 6rpx;
  font-size: 22rpx;
  color: #B76447;
  line-height: 1.5;
}

.withdraw-record-side {
  display: flex;
  flex-direction: column;
  align-items: flex-end;
  flex-shrink: 0;
  padding-top: 2rpx;
}

.withdraw-record-amount {
  font-size: 32rpx;
  font-weight: 700;
  color: #B76447;
}

.withdraw-record-actual {
  font-size: 20rpx;
  color: #B0A396;
  margin-top: 6rpx;
}

.withdraw-empty {
  padding: 36rpx 0 22rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
}

.withdraw-empty-title {
  font-size: 26rpx;
  font-weight: 600;
  color: #78716C;
  margin-bottom: 8rpx;
}

.withdraw-empty-sub {
  font-size: 22rpx;
  color: #B0A396;
  line-height: 1.5;
}

/* ── 佣金流水 ── */
.log-item {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  padding: 22rpx 0;
  border-bottom: 1rpx solid rgba(122, 108, 94, 0.08);
}

.log-item:last-child { border-bottom: none; }

.log-left { flex: 1; margin-right: 16rpx; }

.log-type-row {
  display: flex;
  align-items: center;
  gap: 10rpx;
  margin-bottom: 6rpx;
}

.log-type { font-size: 28rpx; font-weight: 600; color: #1C1917; }

.log-status-badge {
  font-size: 20rpx;
  padding: 4rpx 12rpx;
  border-radius: 999rpx;
}

.log-status-frozen          { color: #B76447; background: #FAECE8; }
.log-status-pending_approval { color: #A06C46; background: #F8EEE4; }
.log-status-approved        { color: #6A7E9A; background: #EEF4FB; }
.log-status-settled         { color: #53725B; background: #EDF6EF; }
.log-status-cancelled       { color: #9A8D80; background: #F2EFEC; }

.log-time { display: block; font-size: 22rpx; color: #B0A396; margin-bottom: 4rpx; }

.log-deadline { font-size: 20rpx; color: #C5A884; }

.log-right { flex-shrink: 0; padding-top: 2rpx; }

.log-amount { font-size: 32rpx; font-weight: 700; }

.log-positive { color: #5A8060; }
.log-negative { color: #B76447; }

/* ── 空状态 ── */
.empty-state {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 60rpx 0 40rpx;
}

.empty-text { font-size: 28rpx; color: rgba(255, 255, 255, 0.5); font-weight: 600; margin-bottom: 10rpx; }
.empty-sub { font-size: 24rpx; color: rgba(255, 255, 255, 0.3); }

/* ── 底部说明 ── */
.footer-note {
  margin: 24rpx 24rpx 0;
  padding: 0 4rpx;
}

.footer-note-text {
  display: block;
  font-size: 22rpx;
  color: rgba(255, 255, 255, 0.3);
  line-height: 1.7;
}

/* ── 提现弹窗 ── */
.popup-mask {
  position: fixed;
  inset: 0;
  background: rgba(0, 0, 0, 0.5);
  z-index: 1000;
  display: flex;
  align-items: flex-end;
  justify-content: center;
}

.popup-panel {
  width: 100%;
  background: #FFFFFF;
  border-radius: 32rpx 32rpx 0 0;
  padding: 36rpx 32rpx calc(40rpx + env(safe-area-inset-bottom));
  box-sizing: border-box;
}

.popup-header {
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
  margin-bottom: 8rpx;
}

.popup-title {
  font-size: 32rpx;
  font-weight: 700;
  color: #1C1917;
}

.popup-close {
  position: absolute;
  right: 0;
  font-size: 44rpx;
  color: #B0A396;
  line-height: 1;
}

.popup-balance-tip {
  font-size: 24rpx;
  color: #9A8D80;
  text-align: center;
  margin-bottom: 16rpx;
}

.popup-balance-num { font-weight: 700; color: #1C1917; }

.popup-rule-tip {
  margin-bottom: 24rpx;
  font-size: 22rpx;
  line-height: 1.6;
  color: #8B7355;
  text-align: center;
}

.popup-input-wrap {
  display: flex;
  align-items: center;
  border-bottom: 2rpx solid rgba(122, 108, 94, 0.15);
  padding: 16rpx 0 20rpx;
  margin-bottom: 20rpx;
}

.popup-input-symbol { font-size: 40rpx; color: #1C1917; font-weight: 600; margin-right: 10rpx; }

.popup-input { flex: 1; font-size: 48rpx; font-weight: 700; color: #1C1917; height: 72rpx; }

.popup-calc-tip {
  margin-bottom: 28rpx;
  font-size: 22rpx;
  color: #8B7355;
  text-align: center;
}

.popup-btn-row { display: flex; gap: 16rpx; }

.popup-cancel {
  flex: 1;
  height: 88rpx;
  border-radius: 999rpx;
  background: #F2EEE8;
  display: flex;
  align-items: center;
  justify-content: center;
}

.popup-cancel-text { font-size: 28rpx; font-weight: 600; color: #78716C; }

.popup-confirm {
  flex: 2;
  height: 88rpx;
  border-radius: 999rpx;
  background: linear-gradient(135deg, #00d2ff 0%, #7b2ff7 100%);
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 8rpx 24rpx rgba(0, 210, 255, 0.3);
}

.btn-loading { opacity: 0.6; }

.popup-confirm-text { font-size: 28rpx; font-weight: 600; color: #FFF7EF; }

/* ── 骨架屏 ── */
.skeleton-wrap { padding-top: 4rpx; }

.sk-log {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  padding: 22rpx 0;
  border-bottom: 1rpx solid rgba(122, 108, 94, 0.06);
}

.sk-info { flex: 1; margin-right: 20rpx; }

.sk-amount {
  width: 80rpx;
  height: 32rpx;
  background: linear-gradient(90deg, #F3EEE9 25%, #EBE2D8 50%, #F3EEE9 75%);
  background-size: 200% 100%;
  animation: shimmer 1.4s ease-in-out infinite;
  border-radius: 10rpx;
}

.sk-line {
  height: 24rpx;
  background: linear-gradient(90deg, #F3EEE9 25%, #EBE2D8 50%, #F3EEE9 75%);
  background-size: 200% 100%;
  animation: shimmer 1.4s ease-in-out infinite;
  border-radius: 10rpx;
  margin-bottom: 12rpx;
}

.sk-long { width: 65%; }
.sk-short { width: 40%; height: 20rpx; }

@keyframes shimmer {
  0%   { background-position: 200% 0; }
  100% { background-position: -200% 0; }
}
