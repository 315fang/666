# 管理端 Vite（复制为 .env.local 后修改）
# 腾讯地图 JS API GL Key：仅用于地图底图显示与点选交互。
# WebService（地址解析 / 逆地理解析 / 地点搜索）已改为走 admin-api 服务端代理，请在 admin-api 环境中配置 TENCENT_MAP_KEY。
# 生产构建时写入此变量后再 npm run build；本地联调若用浏览器打开地图，也需要在腾讯控制台给当前域名配置 JS API 白名单。
VITE_TENCENT_MAP_KEY=
VITE_ADMIN_API_BASE_URL=/admin/api
VITE_ADMIN_ASSET_BASE_URL=
VITE_ADMIN_DEV_PROXY_TARGET=http://127.0.0.1:3200
