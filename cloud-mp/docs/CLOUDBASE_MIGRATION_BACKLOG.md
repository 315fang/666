# CloudBase Migration Backlog

## P0

- 将 `cloudbase-seed` 转换为 CloudBase 可导入 JSONL
- 形成正式导入说明和回滚说明
- 接 CloudBase 正式支付
- 补支付回调与订单状态幂等

## P1

- 后台 CloudRun 服务切换为“标准模型优先”
- 逐步淘汰 `mysql/jsonl` 运行时依赖
- 管理员权限中间件按模块落地
- 用户资料、地址、订单金额字段彻底统一
- 执行旧字段审计并清理页面层 `image_url` / `avatar_url` / `nickname`

## P2

- 素材表改成 `file_id` 主引用
- 商品主图、Banner、活动图全部改用素材库引用
- 小程序展示层统一临时链接转换

## P3

- 分销能力迁移
- 提现与佣金治理
- 拼团 / 砍价 / 抽奖等营销系统治理

## 风险点

- 前端仍有旧状态名依赖
- 前端和后台页面仍有旧图片字段依赖
- 老数据缺失 `skus`，目前使用默认 SKU 回填
- 管理员密码只在旧导出中可验证，真实生产密码仍需环境确认
