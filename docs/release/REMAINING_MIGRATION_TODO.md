# 剩余迁移待办总表

更新日期：2026-04-08

## 说明

这份清单只列还没有迁完、没有对齐、或线上仍不稳定的部分。

状态定义：

- `done`：已完成并通过当前轮验证
- `partial`：已做一部分，但还没达到生产要求
- `todo`：尚未迁移或尚未接入
- `blocked`：已发现明确阻塞，需要优先处理

## P0 生产阻塞

| 模块 | 状态 | 当前问题 | 下一步 |
| --- | --- | --- | --- |
| 正式支付 | partial | 已移除 simulation 分支，当前只保留 formal / disabled，仍缺真实端到端验证 | 做真机支付、真实微信回调、退款链路、金额单位校验 |
| 物流查询 | blocked | `order` 仍有物流占位 | 接真实物流服务 |
| 后台支付健康检查 | done | 已改为真实支付配置检查 | 后续只补更细粒度探测 |
| 用户管理接口 | done | 已重构为轻量聚合列表，线上返回正常 | 后续继续清理旧字段 |

## 后台业务域

| 模块 | 状态 | 当前问题 | 下一步 |
| --- | --- | --- | --- |
| 商品管理 | done | 已可用 | 后续只做字段收口 |
| 分类管理 | done | 已可用 | 后续只做字段收口 |
| 订单管理 | partial | 主列表可用，但支付/物流未闭环 | 补支付与物流 |
| 素材管理 | partial | 新上传已走 CloudBase，历史资产仍混 COS | 继续 `file_id` 化 |
| Banner / 内容位 | partial | 新图可上 CloudBase，旧 `image_url` 仍多 | 改为 `file_id` 主写 |
| 用户管理 | done | 列表、详情、团队汇总已可用 | 后续补更多统计与字段清理 |
| 提现审核 | done | 可用 | 后续补更多统计 |
| 退款审核 | done | 可用 | 后续补更多联动 |
| 佣金结算 | done | 可用 | 后续补更完整规则 |
| 经销商管理 | partial | 已补最小列表/审批/拒绝/等级接口 | 后续补独立数据模型与详情字段 |
| 分支代理 | partial | 已有策略/点位/申请前端契约，服务端需补齐并对齐返回结构 | 补策略、点位、申请审核最小闭环 |
| N路径代理 | todo | 前端依赖 leaders/members/leader-members/upgrade-applications/review | 补最小列表与审核接口 |
| 管理员与权限 | todo | 前端已有完整账号页，服务端仍缺增删改查与改密接口 | 补 `admins` 模块 |
| 运维监控 | done | 已补系统状态、异常、cron、日志与存储检查面板所需接口 | 后续只做字段收口 |

## 小程序业务域

| 模块 | 状态 | 当前问题 | 下一步 |
| --- | --- | --- | --- |
| 登录 | done | 已迁 CloudBase 主路径 | 后续清兼容字段 |
| 商品 | done | 主链路可用 | 继续清兼容字段 |
| 购物车 | done | 主链路可用 | 继续清兼容字段 |
| 订单 | partial | 订单可查，但支付/物流未闭环 | 接支付和物流 |
| 用户中心 | partial | 主页面可用，但字段兼容仍多 | 继续清理 |
| 团队 / 分销 | partial | 主页面协议有了，但高级规则未完成 | 补代理高级规则 |
| 钱包 / 货款 | partial | 页面能出协议，但正式支付没接 | 接正式资金闭环 |
| 活动营销 | partial | 有些页面能跑，但仍有占位与旧模型 | 按优先级收口 |

## 数据与资产

| 模块 | 状态 | 当前问题 | 下一步 |
| --- | --- | --- | --- |
| CloudBase 导入包 | done | 导入包已生成并校验通过 | 等正式环境导入闭环 |
| 旧字段兼容 | blocked | `quantity/user_id/image_url/avatar_url/nickname` 仍很多 | 分域清理 |
| 历史图片资产 | partial | 旧 COS / 外链仍大量存在 | 新资产强制上 CloudBase，旧资产逐步收口 |
| 素材字段规范 | partial | 新上传已用 `file_id`，旧表仍混 `image_url` | 全量内容域改 `file_id` 主写 |

## 当前执行顺序

1. 补 `admins`
2. 补 `n-system`
3. 补 `branch-agents / ops-monitor`
4. 接正式支付
5. 接真实物流
6. 继续清理旧字段与 `file_id` 主写

## 2026-04-08 当日晚些时候新增进展

- 后台订单页已完成一轮关键对齐：
  - 订单金额统一按“分”存储、“元”展示
  - 订单改价前端改为按“元”输入、按“分”提交
  - 订单买家匹配补上 `openid / buyer_id / user_id / _legacy_id` 多口径查找
  - 订单商品改为优先读取 `items[]` 与快照字段，不再只依赖 `order.product_id`
- 当前订单管理仍保持 `partial`，原因没有变：
  - 正式支付未端到端完成
  - 真实物流未接入
