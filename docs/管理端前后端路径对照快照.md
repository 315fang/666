# 管理端前后端路径对照快照（自动生成）

生成命令：`node scripts/audit-admin-api-alignment.mjs`

## 统计

- 前端 API 模块解析到的路径数：154
- 后端 admin 路由解析到的路径数：197

## 前端有而后端未粗匹配（0）

_（无）_

## 后端有而前端 API 模块未粗匹配（28）

可能原因：尚未做管理页、由子路由挂载（本脚本未拼接 `router.use` 前缀，故 `/:id` 等相对路径会出现在此列表）、测试/运维专用接口。

- `/:id`
- `/active`
- `/anomalies`
- `/auto-switch`
- `/cleanup`
- `/cron-status`
- `/db-ping`
- `/env-compare`
- `/env-config/:id`
- `/env-content`
- `/env-report`
- `/env-report/health`
- `/env-template`
- `/env-template/download`
- `/export`
- `/logs/stats`
- `/process`
- `/refresh-cert`
- `/rules`
- `/statistics`
- `/storage/signature`
- `/switch`
- `/test/clear`
- `/test/create-order`
- `/test/create-user`
- `/test/users`
- `/test/users/:id`
- `/upload/multiple`

## 说明

- admin 实际挂载在 `/admin/api`（或等价前缀）下；此处只比较从源码解析出的路径字面量形态。
- 若路径含多级动态参数，需人工核对。
